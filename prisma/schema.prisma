generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model shortenedUrl {
  id           String @id @default(cuid())
  targetUrl    String
  shortenedUrl String
  shortCode    String
}
